<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    tools:context="com.project2.taes.farmacia.ConfiguracionActivity">

        <include layout="@layout/toolbar"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">
    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_logo"
        android:scaleType="fitXY"
        android:layout_alignParentTop="true"
        android:layout_marginTop="30dp"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:padding="10dp"
        android:id="@+id/linearLayout">
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/usuario"
            android:padding="10dp"
            android:hint="@string/usuario"
            android:text="usuario@farmacia"
            android:background="@drawable/edit_text_arriba_conf"
            android:drawableLeft="@drawable/ic_usuario"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:id="@+id/contrasenya"
            android:padding="10dp"
            android:hint="@string/contrasenya"
            android:text="123456"
            android:background="@drawable/edit_text_medio_conf"
            android:drawableLeft="@drawable/ic_contrasenya"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/servidor"
            android:layout_gravity="center_horizontal"
            android:padding="10dp"
            android:hint="@string/servidor"
            android:text="ESP2"
            android:background="@drawable/edit_text_abajo_conf"
            android:drawableLeft="@drawable/ic_servidor"/>

        <Button
            android:id="@+id/guardar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:layout_margin="4dp"
            android:text="@string/guardar"
            android:textColor="#fff"
            android:background="@drawable/button_guardar_conf"
            android:onClick="guardar"/>
    </LinearLayout>
    </LinearLayout>
</RelativeLayout>
